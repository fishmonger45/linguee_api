services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      LINGUEE_REDIS_URL: redis://redis:6379/0
      LINGUEE_LOG_FORMAT: console
    depends_on:
      - redis
